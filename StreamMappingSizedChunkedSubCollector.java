import java.util.stream.Collector;
import java.util.stream.Stream;

public class StreamMappingSizedChunkedSubCollector {

    public static <T,A,R> Stream<R> chunked(Stream<T> src, int splitSize, Collector<T,A,R> chunkCollector){
        return chunked

        return (Stream<R>) chunkCollector;// dummy response
    }
}
